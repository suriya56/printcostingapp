const admin = require('firebase-admin');

// We don't have the service account key easily here, but we can hit the API endpoint since it's running?
// Wait, maybe we can just make a small node script using the app's apiFetch?
// Actually we can't because it requires a token.
